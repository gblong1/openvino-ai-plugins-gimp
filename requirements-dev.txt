# Development and testing dependencies for GIMP OpenVINO AI Plugins

# Testing framework
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
pytest-mock>=3.11.1
pytest-timeout>=2.1.0

# Code quality - Modern tools (recommended)
ruff>=0.8.0           # Fast Python linter and formatter (replaces black + flake8)
pre-commit>=3.5.0     # Git hooks framework for automated checks

# Code quality - Legacy tools (kept for backward compatibility)
black>=23.0.0         # Code formatter (use ruff format instead)
flake8>=6.0.0         # Linter (use ruff check instead)
isort>=5.12.0         # Import sorter (works with ruff)
mypy>=1.4.0           # Type checker

# Additional testing utilities
coverage>=7.2.0

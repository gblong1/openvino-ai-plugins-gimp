# Development and testing dependencies for GIMP OpenVINO AI Plugins

# Testing framework
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
pytest-mock>=3.11.1
pytest-timeout>=2.1.0

# Code quality - Modern linting and formatting
ruff>=0.3.0              # Fast Python linter and formatter (replaces flake8, pylint, etc.)
black>=23.0.0            # Code formatter (ruff can replace this, kept for compatibility)
isort>=5.12.0            # Import sorting (also built into ruff)
mypy>=1.4.0              # Type checking

# Pre-commit hooks
pre-commit>=3.6.0        # Git hook framework for automated checks

# Additional testing utilities
coverage>=7.2.0
